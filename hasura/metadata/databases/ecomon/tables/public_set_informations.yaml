table:
  name: set_informations
  schema: public
array_relationships:
  - name: sets
    using:
      foreign_key_constraint_on:
        column: set_information_id
        table:
          name: sets
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - corrupted_record_count
        - id
        - record_count
        - record_regime_pause_duration
        - record_regime_recording_duration
        - end_date
        - start_date
        - record_duration
        - remarks
        - first_record
        - last_record
        - created_at
        - updated_at
      filter: {}
      allow_aggregations: true
    comment: ""

http:
  routers:
    airflow-router:
      rule: "Host(`localhost`) && PathPrefix(`/ecomon_next/airflow`)"
      service: airflow-service
      entryPoints:
        - web

  services:
    airflow-service:
      loadBalancer:
        servers:
          - url: "http://airflow:8080"
        passHostHeader: true
